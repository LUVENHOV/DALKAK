// const { StrictMode } = require('react');

const nextConfig = {
  StrictMode: false,
  distDir: 'build',
  output: 'export',
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;
